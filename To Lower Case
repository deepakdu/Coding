class Solution {
public:
    string toLowerCase(string str) {
        for(int i=0;i<str.length();i++){
            if(int(str[i])>=65 && int(str[i])<=90){
                int s=int(str[i]);
                s=s+32;
                str[i]=char(s);
            }
        }
        return str;
    }
};
